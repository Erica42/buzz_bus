<div>Select your Destination:</div>
<form>
<select id="select_location" name="destination">
  <% all_locations.each do |location| %>
  <option value="<%= [location[1], location[2]] %>"><%= location[0] %></option>
  <% end %>
  </select>
  <button class='button' id='set_destination' type="submit">Set Destination</button>
</form>

